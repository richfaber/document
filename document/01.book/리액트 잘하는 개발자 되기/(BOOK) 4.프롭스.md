# 4.프롭스

프롭스는 상위에서 하위로 구성하는 전달인자 이고, 다음과 같은 특징이 있다.

1. 프롭스는 객체다
2. 부모 컴포넌트가 자식 컴포넌트로 데이터를 전달할 때 프롭스를 사용한다.
3. 프롭스에는 값, 배열, 객체, 함수 등 다양한 데이터를 담아 전달할 수 있다.
4. 프롭스는 읽기 전용이다. 부모 컴포넌트에서만 값을 수정하고 조작할 수 있고, 자식 컴포넌트 에서는 프롭스를 수정할 수 없다.

